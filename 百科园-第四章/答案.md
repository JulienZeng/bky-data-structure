## 第一题

```c++
		//暴力解决法
		/**********Begin**********/
		for(int i=0;i<10;i++)
		{
			if( str[k] == ('0'+i) )
				num[i]++;
		}
		for(int i=0;i<26;i++)
		{
			if( str[k] == ('A'+i) )
				num[10+i]++;
		}
		/**********End**********/
```

## 第二题

```c++
	//数据结构 P92[算法描述] 原代码
	/**********Begin**********/	
	while(i<=S.length && j<= T.length)
	{
		if(S.ch[i] == T.ch[j])
		{
			++i;
			++j;
		}
		else
		{
			i = i-j+2;
			j = 1;
		}	
	}
	/**********End**********/
```

