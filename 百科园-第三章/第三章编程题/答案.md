# 1  
```
//算法3.15　取循环队列的队头元素
QElemType GetHead(SqQueue Q) {//返回Q的队头元素，不修改队头指针

	/**********Begin**********/
	if (Q.front == Q.rear)
		return ERROR;
	return Q.base[Q.front];

/**********  End  **********/	
```